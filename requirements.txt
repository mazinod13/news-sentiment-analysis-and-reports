# Runtime dependencies.
#
# pyproject.toml is the source of truth -- this file mirrors its
# [project.dependencies] and tests/test_packaging.py fails if the two drift.
# When you add a dependency, edit BOTH, in the same commit.
#
#   pip install -r requirements.txt          # runtime only
#   pip install -r requirements-dev.txt      # runtime + test/lint tooling
#
# These are minimum-version constraints, not a lockfile. For byte-identical
# rebuilds, generate one from a known-good environment:
#   pip freeze > requirements.lock.txt

httpx>=0.27
# certifi arrives transitively via httpx, but fetcher.py imports it directly
# to build the TLS trust bundle -- declare what we import.
certifi>=2024.7.4
feedparser>=6.0.11
selectolax>=0.3.21
PyYAML>=6.0
python-dateutil>=2.9
nepali-datetime>=1.0.8
# The zoneinfo database as a package: Alpine ships no /usr/share/zoneinfo, and
# Asia/Kathmandu (+05:45) must resolve or every timestamp is wrong.
tzdata>=2024.1
SQLAlchemy>=2.0.30
# PyMySQL is pure Python and 0.15 MB installed. `cryptography` is deliberately
# NOT a dependency: MySQL 8's caching_sha2_password needs it only on an
# unencrypted connection, and it costs ~16 MB with cffi. Connect with TLS
# (MYSQL_SSLMODE) instead -- see app/storage/db.py.
PyMySQL>=1.1
# The read-only HTTP API. Starlette + uvicorn is ~2 MB installed; FastAPI would
# add Pydantic v2, whose compiled core is larger than everything else here.
starlette>=0.37
uvicorn>=0.30
