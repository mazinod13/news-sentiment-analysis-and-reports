# Selector pack for Janakpur Today article pages.
# Syntax: a CSS selector, optionally suffixed with @attribute.
#   "p.date span"                        -> text of the first match
#   "meta[property='og:image']@content"  -> that attribute
# Verify against tests/fixtures/ejanakpurtoday_story.html before committing.

article:
  title: "h2.post-title.title-xl.mb-20"
  body: "div.post-header-area"                # container holding the article paragraphs
  author: "a.author"
  published: "div.post__time > span"
  # bs = Bikram Sambat in Devanagari, iso = machine-readable, auto = try both
  published_format: auto
  image: "meta[property='og:image']@content"

exclude:
  - "script"
  - "style"
  - "aside"
  # - ads, related-story rails, poll widgets, share bars...
